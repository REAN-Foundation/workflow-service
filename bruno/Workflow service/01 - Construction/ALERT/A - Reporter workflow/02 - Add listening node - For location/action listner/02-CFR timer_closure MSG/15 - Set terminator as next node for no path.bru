meta {
  name: 15 - Set terminator as next node for no path
  type: http
  seq: 15
}

put {
  url: {{BASE_URL}}/engine/nodes/{{NOCFR_TIMER}}/next-node/{{OVERALL_TERMINATOR}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  x-api-key: {{API_KEY}}
  Authorization: Bearer {{MODERATOR_USER_JWT}}
}

tests {
  test("Request is successfull", function () {
      expect(res.getStatus()).to.equal(200);
      var jsonRes = res.getBody();
      expect(jsonRes.Status).to.eql('success');
  });
}

docs {
  Set EN2 as next node for the No path of CFR availability check.
  
  When CFRs are available (array not empty), IsEmpty condition = false, triggers NoAction = Continue to EN2.
}
