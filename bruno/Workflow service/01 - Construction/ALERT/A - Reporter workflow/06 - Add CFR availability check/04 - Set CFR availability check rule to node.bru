meta {
  name: 04 - Set CFR availability check rule to node
  type: http
  seq: 6
}

put {
  url: {{BASE_URL}}/engine/nodes/{{MAIN_CFR_AVAILABILITY_CHECK_ID}}/base-rule/{{CFR_AVAILABILITY_CHECK_RULE_ID}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  x-api-key: {{API_KEY}}
  Authorization: Bearer {{MODERATOR_USER_JWT}}
}

tests {
  test("Request is successfull", function () {
      expect(res.getStatus()).to.equal(200);
      var jsonRes = res.getBody();
      expect(jsonRes.Status).to.eql('success');
  });
}

docs {
  Set CFR availability check rule to the decision node.
  
  This assigns the rule to the CFR availability check decision node.
}
