meta {
  name: 09 - Set Timer as next node of No CFR EN
  type: http
  seq: 9
}

put {
  url: {{BASE_URL}}/engine/nodes/{{NOCFR_EN_ID}}/next-node/{{NOCFR_TIMER_ID}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  x-api-key: {{API_KEY}}
  Authorization: Bearer {{MODERATOR_USER_JWT}}
}

tests {
  test("Request is successfull", function () {
      expect(res.getStatus()).to.equal(200);
      var jsonRes = res.getBody();
      expect(jsonRes.Status).to.eql('success');
  });
}

docs {
  Set timer node as next node of No CFR execution node.
  
  This connects the No CFR execution node to the timer for the delay.
}
